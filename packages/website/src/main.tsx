import React, { StrictMode } from 'react';
import ReactDOM from 'react-dom';
import { Button } from '@toys/toys-ui';

ReactDOM.render(
  <StrictMode>
    <div>这是一个组件,hahah</div>
    <Button />
  </StrictMode>,
  document.getElementById('root'),
);
